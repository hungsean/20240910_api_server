services:
  api-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - ASSETS_PATH=/app/assets
    volumes:
      - ./assets:/app/assets
      - ./src:/app/src
    command: python src/__main__.py